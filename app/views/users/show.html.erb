<div class="main user-show">
  <div class="container">
    <div class="user">
      <h2>名前<%= @user.name %></h2>
      <p>email: <%= @user.email %></p>
      <p>性別:   <%= @user.sex %></p>
      <p>年齢:   <%= @user.age %></p>
      <h5><%= link_to("編集", "/users/#{@user.id}/edit") %></h5>
    </div>
  </div>
</div>
